module.exports = {
  rules: {
    // 对象末尾的逗号：多行的对象可以加可以不加，单行的一定不能加
    // airbnb推荐值：es6: ['error', 'always-multiline'], es5: ['error', 'never']
    // 即ES6中需要使用末尾逗号，ES5中不允许使用末尾逗号；单行的对象，不允许使用末尾逗号
    'comma-dangle': ['error', 'never'],
  },
};
